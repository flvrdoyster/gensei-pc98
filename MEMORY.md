# 환세포물장(幻世捕物帳) 한국어 번역 작업 지침

> Compile PC-98 환세 시리즈 팬번역. 작업자 '굴'(GitHub: flvrdoyster)이 최종 확정.
> 이 문서는 다른 클로드 세션이 작업을 이어받을 때를 위한 자족적 지침이다.
> GUIDE.md(저장소 내 확정 용어·포맷·말투 9장)가 항상 정본이며, 이 문서는 그 위의 작업 태도·반복 실수 교정을 담는다.

---

## 0. 가장 중요한 것 — 작업의 목표

목표는 **"빠른 반환"이 아니라 "굴의 손이 최소한으로 가는, 의미가 온전한 번역"**이다.
표를 빨리 내놓는 것은 작업의 단위가 아니다. 굴이 확정본을 주는 것은 작업 속도를 절반으로 깎으면서까지 주는 **학습 재료**다 — 차이를 나열하고 넘어가지 말고, 그 교정이 드러내는 패턴을 다음 블록에 선반영해 같은 교정을 반복하게 만들지 마라.

---

## 1. 매 블록 진입 절차 (이 순서 고정)

**글자수 판정 스크립트를 첫 동작으로 돌리지 마라.** 글자수부터 돌리면, 스크립트에 넣기 위해 급조한 1차 번역이 글자수만 통과하고 그대로 굳어버린다. 의미·정착표현·말투 점검이 거기에 묻힌다. 이것이 최대 반복 실수의 근원이다.

1. **GUIDE.md + 해당 블록 화자 말투·확정 용어를 먼저 확인** (메모리/기억에만 의존 금지)
2. **스크립트 없이 직역으로 의미부터 확정** — 한국에서 굳은 정착 표현을 우선한다. 한자를 그대로 옮기기 전에 *"한국에선 이걸 뭐라고 쓰지?"* 를 반드시 자문.
   - 예: 神隠し = 행방불명 (× 신감춤), 力士 = 스모꾼 (× 역사/장사/력사)
3. **말투·용어·자연스러움까지 번역을 제대로 완성** — 실제로 안 쓰는 표현 금지 (예: '일렀구먼' × → '금방 왔구먼')
4. **그제서야 완성본을 글자수 함수에 넣어 검증** — 글자수는 마지막 거름망·상한선이지 출발점이 아니다.
5. **초과한 줄은 두 안을 모두 제시하고 굴의 확인을 받는다** — ① 글자수 무시한 '의미·자연스러움 최적안' ② 글자수 맞춘 '압축안'. 임의로 압축안만 골라서 가져가지 마라.

---

## 2. 번역 원칙 (직역 우선 + 글자수 + 행·블록)

**우선순위: 원문충실 > 자연스러움 > 글자수.** 의미가 손상되면 글자수를 무시한다(둘 다 제시·확인).

### 직역 우선
- 의역·압축은 마지막 수단. 핵심어를 건드리기 전에 생략 가능한 지시어('그')·조사·군더더기부터 제거.
- 관용구는 압축으로 깨지 마라 (식은 죽 먹기; '식은 죽' 불가).
- 직역되는 걸 한국어 관용/의역으로 갈아끼우지 마라.
- 동사 직역/의역은 게임 내 실제 연출을 보고 결정.

### 글자수 (마지막 거름망)
- 「도 1자, 띄어쓰기 포함, 앞 전각공백(　)만 제외(그 자리도 버퍼).
- 줄별로 코드 판정 (눈으로 OK 찍다 통과시키는 게 반복 실수).
- 거름망은 상한선이지 '되도록 짧게'가 아니다. 여유 폭이 크면 그 폭으로 원문 뉘앙스(서법 〜そう/〜てしまう 추측·완료, 조사 が·を, 何か 등)를 더 채울 게 없나 역점검 — 짧으면 납작해진다.

판정 함수 예시:
```python
def w(s): return len(s.lstrip('　'))   # 앞 전각공백만 제외
def J(o, jp, kr):
    j, k = w(jp), w(kr)
    return f'{o} {"OK" if k<=j else "★초과"} 원{j} 번{k} | {kr}'
```

### 행·블록 처리
- 행마다 독립 종결시키는 버릇 금지. 한국어는 줄을 넘겨 관형절로 한 문장으로 엮는 게 자연스럽다 (연체수식이 다음 줄로 넘어가면 '~하는 ○○'으로, 종결로 끊지 마라).
- 두 줄 대사는 원문 행 순서에 매이지 말고 한국어 자연 어순으로 재배분.
- 블록 전체로 판단 — 뒷줄 동사가 이미 함의하는 동작·어절은 빼서 핵심어 직역을 살린다 (力を持った玉 → 봉인하다에 함의되므로 '힘의 옥'이 아니라 '봉인하는 옥').
- 体言止め(명사 종결)라도 한국어는 동사 평서종결이 자연 (技를 '베기'로 욱여넣지 마라). 단 아이템명이 이미 그 사물이면 중복 명사 빼고 형용 종결.

---

## 3. 조사·어휘·서법 반복 실수

- **조사 が(이/가)·を(을/를)를 글자수 줄이려 습관적으로 생략 금지** — 핵심어를 짧은 동의어로 바꿔 공간을 확보한 뒤 조사를 살려라.
- 은/는·이/가·을/를은 **발음 기준** (30000G 뒤 = '는').
- 전반적으로 문어·격식 쪽으로 기우는 경향을 경계.
- **원문 서법을 평탄화 금지** (추측 らしい, 존경 수동 言われる, 정중체 のか 등). 거친 쪽으로도 정중 쪽으로도 납작하게 만들지 마라. 어미표는 가이드일 뿐, 원문 서법·맥락이 우선 (そうか는 정중체 화자라도 혼잣말 납득이면 '그렇군').
- 단, 캐릭터 톤을 모든 어휘에 기계적으로 욱여넣는 **과보정도 금지** — 핵심 표현만 결을 살리고 나머지는 담백하게.

---

## 4. 말투·고유명 (GUIDE가 정본 — 매 블록 먼저 확인)

### 캐릭터별 말투 (★ = 반복 교정 대상)

- **화린** (정중체 여성): ですわ/ですわね/かしら의 기본값은 담백한 '~어요'(안심이에요/놀라워요). ★기계적 '~네요' 금지 — '~네요'는 감탄·동의가 또렷한 줄에만. 혼잣말 납득·깨달음은 '~군요'/'~겠어요'. かしら 등 혼잣말도 정중체 '~요' 유지(그랬나 → 그랬나요).
- **유리와카마루** (남장 여검사): ★'~군' 기본값 금지(깨달음·발견 자리에만). 평서·단정 '~다/~야', 납득·동의 '~지', 가벼운 감상 '~네/~인데', 추측 '~겠지/~ㄴ 듯해', 의문 '~나/~인가'. 1인칭 おれ = 나. 兄上 = 형님. 평소엔 남자 말투지만 당황·동요로 말투가 무너지는 때를 살린다.
- **스마슈** (경박한 반말, 호색 개그): オレ = 나. 화린쨩(쨩 붙여쓰기). 자뻑·수작 톤 유지.

### 고유명·호칭·포맷
- char 태그(화자명)·고유명·호칭은 이미 번역돼 있는 경우가 많다. **멋대로 새로 만들지 말고 json 기번역을 먼저 확인**해 맞춰라.
- 『』는 아이템·고유명 강조 괄호 — 그대로 유지(「」로 바꾸지 마라).
- 호칭 접미사 붙여쓰기·『』 등 포맷·확정용어는 GUIDE가 정본.

---

## 5. 파일 메모

- 작업 파일: `translation.json` — `entries[] → lines[]` 구조. tag: None=본문, char(화자명)/item/menu/monolog, cutscene=챕터 구분.
