# Claude Code 지침

## 커밋

- **커밋은 사용자가 직접 판단한다. Claude는 자동으로 커밋하지 않는다.**
  - 변경은 작업 트리에 남겨두고, 커밋 여부·시점·범위는 사용자가 결정.
  - 사용자가 "커밋해" 등으로 명시할 때만 커밋한다.
- 커밋 메시지는 한국어로.
- 커밋 시 README.md, 관련 NOTES.md 최신화. README는 변경한 부분만이 아니라 **전체 내용**을 검토해서 낡은 내용이 없는지 확인할 것.
- **실험 전 커밋 제안**: 불확실한 변경(빌드 플래그·구조 변경·새 시도) 전에는 현재 동작 상태를 먼저 커밋해 두도록 사용자에게 제안한다(직접 커밋하지는 않음).

## 수정 전 확인

코드나 파일을 수정하기 전에 무엇을 어떻게 바꿀지 먼저 말하고 확인받을 것.

- 수정 전에 "변경 범위: X, Y, Z — 진행할까?" 형태로 확인.
- 단순 오타 수정이나 명백한 버그 픽스는 예외.
- 구조 변경, 새 기능 추가, 빌드 설정 변경은 반드시 확인 후 진행.

## 파서/스크립트 실행 주의

파서 실행은 translation.json을 덮어쓰는 파괴적 행위다.

- 파서 실행 전, translation.json에 kr 값이 있는지 먼저 확인.
- kr 값이 있으면 사용자에게 알리고 명시적 승인 후 실행.
- "현재 상태 확인"이 목적이라도 파서를 실행하면 파일이 바뀐다. 읽기만 할 거라면 json을 직접 읽을 것.

## 문서 작성 원칙

- **수치 금지**: 진행률(~35%), 엔트리 수, 버전 번호 등 금방 낡는 값은 문서에 쓰지 않는다.
- **추측 금지**: 검증되지 않은 방법이나 경로는 노트에 쓰지 않는다. 확인된 사실만 기록.
- **중복 금지**: 코드에 이미 있는 내용(스키마, 상수, 알고리즘)은 노트에서 참조만 한다.
- **개인 경로 금지**: public 문서(NOTES, README 등)에 절대 경로(`/Users/...`)나 로컬 환경 정보를 쓰지 않는다. 플레이스홀더(`<emsdk_path>`) 또는 범용 커맨드(`em-config`)로 대체.
- **독자 중심**: 문서를 쓰기 전에 "이걸 읽을 사람이 누구인가"를 먼저 생각할 것.

## 작업 결과 메모리 저장

새로운 환경 설정, 빌드 방법, 커맨드 등을 발견하거나 처음 실행에 성공했을 때 **즉시** 메모리에 저장할 것.

- 다음 세션에서 같은 걸 처음부터 다시 찾는 건 사용자 시간 낭비다.
- 특히 환경 의존적인 정보(emsdk 경로, 빌드 커맨드, 특이한 설정 등)는 반드시 저장.

## 웹 배포 (GitHub Pages)

- 개발은 `emulator/`, 배포는 `docs/`. GitHub Pages는 `docs/`를 서빙.
- `emulator/` 수정 후 바로 docs에 복사하지 않는다. 사용자가 로컬에서 확인한 뒤 커밋.
- 작업 마무리 시 문서 업데이트와 함께 `docs/` 배포 여부를 사용자에게 확인.
- `docs/` 갱신 시 `emulator/`의 내용을 통째로 덮어쓰기 (`cp -r emulator/* docs/`).
- `emulator/CNAME`에 커스텀 도메인(`pc98.atah.io`)이 포함되어 있으므로 복사 시 자동으로 `docs/`에 반영됨.

### 배포 버전 관리

- `docs/` 퍼블릭 배포는 **레포 통합 버전**(`vX.Y.Z`, 3자리 semver)으로 관리. 게임별 버전 아님. **최종 완성 시 1.0.0**을 목표로, 그 전까진 `0.Y.Z`.
- 버전 표기는 `emulator/version.js`의 `VERSION` 한 곳(단일 소스). 각 게임 페이지가 include해 푸터에 자동 표시. 배포 시 이 값만 올리고 docs 동기화.
- 배포 커밋에 git 태그 `vX.Y.Z` 부여. (`git tag vX.Y.Z` → 푸시 시 `--tags`)
- 의미: X(major)=`0`→최종 완성 시 `1` 이정표 / Y(minor)=게임 추가·기능 추가 / Z(patch)=오타·번역 수정.
- **버전 올리는 규칙**:
  - 사용자가 `docs/` 업데이트를 요청하면 → **Z(patch) 자동 +1** (예: v0.1.0 → v0.1.1). `version.js`만 고치면 됨.
  - 게임 추가·에뮬레이터 구조 리팩토링 등 **규모가 큰 변경**이면 → Y(minor)를 올릴지 **사용자에게 먼저 질문**. 올리면 Z는 0으로 (예: v0.1.3 → v0.2.0).
  - **최종 완성(1.0.0)**도 이정표라 임의로 올리지 말고 **질문**.

## 웹 에뮬레이터 공통 관리 (파편화 금지)

각 게임 페이지(`hukyou.html`, `kitan.html`, `kitan-opening.html`, …)는 **동일한 구조·UI·기능을 공유**한다. 한 페이지만 고치고 나머지를 방치하지 말 것.

- **공통 요소**: top-bar(로고 + `top-bar-right`에 mute·전체화면), 가상 게임패드, 토스트, SVG 아이콘, 세이브 시스템(IndexedDB `gensei-saves`/`disks` + 폴링·복원)을 모든 페이지에서 같은 방식으로 둔다.
- **아이콘**: 텍스트 글리프(⛁ ⛶ 등) 쓰지 말고 다른 페이지와 같은 SVG 사용. mute 버튼은 `id="btn-mute"` 만 두면 `audio.js`가 자동 연결.
- **기능 추가/제거는 전 페이지 일괄 적용**. 예: export/import 제거, 세이브 방식 변경 → 모든 게임 페이지에 동일 반영.
- 새 게임 페이지는 기존 페이지를 복제해서 시작하고, 게임별로 달라지는 부분(디스크 경로·로고·제목·세이브 대상 FDI)만 바꾼다.
- 디스크가 여러 개면 세이브는 파일명 키로 각각 저장하되, **체크섬으로 변경된 디스크만** IDB에 기록.

## 수정 후 확인 흐름

1. `emulator/` 수정
2. 사용자에게 확인 요청 (로컬 서버 또는 스크린샷)
3. 확인받은 후 커밋
4. 작업 마무리 시: 문서 업데이트 + `docs/` 동기화 필요 여부 확인

## 모르면 물어보기

추측에 토큰 낭비하지 말고 모르겠으면 바로 질문.

- 불확실한 기술적 세부사항은 웹 검색이나 추론 전에 사용자에게 먼저 확인.
- 도메인 지식(레트로 게임 해킹, 폰트 포맷 등)은 사용자가 이미 알고 있을 가능성 높음.
- "모른다"의 기준을 낮출 것. 한 번 시도해서 안 되면 바로 질문.
- 빌드 플래그, API 동작 방식처럼 버전/환경에 따라 달라지는 것은 첫 실패 후 즉시 질문. 삽질 반복 = 토큰 낭비.

## 시리즈 작업 — 이전 성공 방법 먼저

같은 시리즈의 다른 타이틀을 분석할 때는, 이전 타이틀에서 성공한 방법을 **먼저** 적용해볼 것.

- 새 타이틀 분석 시작 전: "이전 타이틀에서 쓴 기법이 여기서도 통하는가?" 를 첫 번째 질문으로.
- 압축, 파일 포맷, 제어코드 체계 등은 같은 회사 게임이면 동일하거나 유사할 가능성이 높음.
- 이미 동작하는 코드(`compile_lz.py`, `hukyou_parser.py` 등)를 새 타이틀에 바로 적용해보고, 안 될 때 분기점을 찾을 것.
- 분석 결과가 기대에서 벗어날 때(SJIS 밀도 이상, 노이즈 과다 등) — "이전 타이틀과 같은 방식으로 처리하면?" 을 먼저 시도.
