# Claude Code 지침

## 커밋

- 이 레포에서는 Claude가 직접 커밋 수행. 매번 확인받지 않아도 됨.
- 커밋 메시지는 한국어로.
- 작업 완료 시 README.md, 관련 NOTES.md 최신화 후 커밋.
- **실험 전 커밋**: "이게 될지 모르겠다"는 생각이 드는 변경 전에 반드시 현재 동작 상태를 먼저 커밋. 빌드 플래그 변경, 새 라이브러리 시도, 구조 변경 등이 해당. 실패하면 git checkout으로 즉시 복구 가능해야 한다.

## 수정 전 확인

코드나 파일을 수정하기 전에 무엇을 어떻게 바꿀지 먼저 말하고 확인받을 것.

- 수정 전에 "변경 범위: X, Y, Z — 진행할까?" 형태로 확인.
- 단순 오타 수정이나 명백한 버그 픽스는 예외.
- 구조 변경, 새 기능 추가, 빌드 설정 변경은 반드시 확인 후 진행.

## 모르면 물어보기

추측에 토큰 낭비하지 말고 모르겠으면 바로 질문.

- 불확실한 기술적 세부사항은 웹 검색이나 추론 전에 사용자에게 먼저 확인.
- 도메인 지식(레트로 게임 해킹, 폰트 포맷 등)은 사용자가 이미 알고 있을 가능성 높음.
- "모른다"의 기준을 낮출 것. 한 번 시도해서 안 되면 바로 질문.
- 빌드 플래그, API 동작 방식처럼 버전/환경에 따라 달라지는 것은 첫 실패 후 즉시 질문. 삽질 반복 = 토큰 낭비.
